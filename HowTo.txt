Terminator:
	split window:
	- vertically:   ctrl + shift + o
	- horizontally: ctrl + shift + e
	close window:   ctrl + shift + w


Typescript:
$ tsc --watch
tsconfig.json - settings file contains what to compile to which directory

Express:
$ yarn start

FrontEnd React:
$ cd ../mrsoirfe & yarn start


Heroku:
https://git.heroku.com/mrsoir.git
https://mrsoir.herokuapp.com/
add the remote:
$ heroku git::remote -a https://git.heroku.com/mrsoir.git
-> check if https://git.heroku.com/mrsoir.git was added as a remote to the project:
$ git remote -v
